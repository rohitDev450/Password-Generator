apiVersion: v1
kind: Service
metadata:
  name: passgen
spec:
  selector:
    app: passgen
  type: NodePort  # or NodePort if no LB available
  ports:
    - protocol: TCP
      port: 80         # service port
      targetPort: 80   # container port
      nodePort: 30080
